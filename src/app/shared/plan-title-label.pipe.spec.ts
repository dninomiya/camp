import { PlanTitleLabelPipe } from './plan-title-label.pipe';

describe('PlanTitleLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new PlanTitleLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
