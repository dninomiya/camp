import { ResourceIconPipe } from './resource-icon.pipe';

describe('ResourceIconPipe', () => {
  it('create an instance', () => {
    const pipe = new ResourceIconPipe();
    expect(pipe).toBeTruthy();
  });
});
