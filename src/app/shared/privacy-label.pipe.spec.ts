import { PrivacyLabelPipe } from './privacy-label.pipe';

describe('PrivacyLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new PrivacyLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
