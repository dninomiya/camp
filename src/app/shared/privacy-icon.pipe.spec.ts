import { PrivacyIconPipe } from './privacy-icon.pipe';

describe('PrivacyIconPipe', () => {
  it('create an instance', () => {
    const pipe = new PrivacyIconPipe();
    expect(pipe).toBeTruthy();
  });
});
