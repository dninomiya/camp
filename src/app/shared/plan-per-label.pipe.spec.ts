import { PlanPerLabelPipe } from './plan-per-label.pipe';

describe('PlanPerLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new PlanPerLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
